/* Carrega os ajustes mobile sem alterar a lógica do VM. */
(function(){
  var link=document.createElement('link');
  link.rel='stylesheet';
  link.href='mobile.css?v=20260907';
  document.head.appendChild(link);
})();
